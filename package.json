{
  "name": "lo-gx-playwright-e2e",
  "version": "1.0.0",
  "description": "Lombard Odier — Framework d'automatisation E2E avec Playwright, Cucumber et TypeScript",
  "scripts": {
    "pretest": "npm run clean",
    "test": "npx cucumber-js",
    "test:report": "npm run clean && npx cucumber-js || true && npm run report:open",
    "test:smoke": "npx cucumber-js --profile smoke",
    "test:regression": "npx cucumber-js --profile regression",
    "test:headed": "HEADLESS=false npx cucumber-js",
    "report:generate": "npx allure generate reports/allure-results --clean -o reports/allure-report",
    "report:open": "npm run report:generate && npx allure open reports/allure-report",
    "clean": "rm -rf reports/allure-results/* reports/allure-report reports/screenshots/* reports/videos/* reports/rapport-cucumber.* reports/cucumber-report.*",
    "lint": "npx tsc --noEmit"
  },
  "dependencies": {
    "@cucumber/cucumber": "^10.8.0",
    "@playwright/test": "^1.41.0",
    "allure-cucumberjs": "^3.4.5",
    "allure-js-commons": "^3.4.5",
    "dotenv": "^16.3.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "allure-commandline": "^2.29.0"
  }
}